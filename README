This "class" wraps an ES Harmony Map "class" to allow iteration over the
key-value pairs using a forEach.

```javascript
var map = new ForEachMap;
map.set('a', 'A');
map.set('b', 'B');
map.forEach(function(key, value) {
  console.log(key, value);
});
```